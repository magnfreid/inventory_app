/// Width threshold in logical pixels above which the app switches to the
/// expanded (wide-screen) layout.
const double kExpandedBreakpoint = 600;

/// Maximum width of the main content column in the expanded layout,
/// preventing items from stretching across very wide viewports.
const double kContentMaxWidth = 900;
