import 'package:flutter/material.dart';

class RecycledIcon extends StatelessWidget {
  const RecycledIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.eco,
      color: Colors.green,
      size: 16,
    );
  }
}
