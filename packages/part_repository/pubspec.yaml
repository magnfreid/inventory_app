name: part_repository
description: A Very Good Project created by Very Good CLI.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  core_remote:
    path: ../core_remote
  flutter:
    sdk: flutter
  part_remote:
    path: ../part_remote
  rxdart: ^0.28.0

dev_dependencies:
  build_runner: ^2.11.1
  mocktail: ^1.0.4
  test: ^1.29.0
  very_good_analysis: ^10.2.0

resolution: workspace
