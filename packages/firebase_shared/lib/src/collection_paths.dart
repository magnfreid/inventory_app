///Firestore collection reference for organizations.
const organizationsCollection = 'organizations';

///Firestore collection reference for parts.
const partsCollection = 'parts';

///Firestore collection reference for stock.
const stockCollection = 'stock';

///Firestore collection reference for storages.
const storagesCollection = 'storages';

///Firestore collection reference for tags.
const tagsCollection = 'tags';

///Firestore collection reference for transactions.
const transactionsCollection = 'transactions';

///Firestore collection reference for users.
const usersCollection = 'users';
