import 'package:firebase_image_remote/firebase_image_remote.dart';
import 'package:test/test.dart';

void main() {
  group('FirebaseImageRemote', () {
    test('can be instantiated', () {
      expect(FirebaseImageRemote(), isNotNull);
    });
  });
}
